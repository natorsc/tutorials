swiftly --generate-completion-script bash >> ~/.bash_completion
poetry completions bash >> ~/.bash_completion
pdm completion bash >> ~/.bash_completion
uv generate-shell-completion bash >> ~/.bash_completion
